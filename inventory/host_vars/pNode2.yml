---
prometheus_labels:
  environment: production
  role: database
  datacenter: se-1

postgres_exporter_dsn: "postgresql://exporter:{{ vault_postgres_exporter_password }}@localhost:5432/postgres?sslmode=disable"
