---
# Proxmox scrape configuration.
# The pve_exporter runs on monitoring nodes and queries this target remotely.

proxmox_job_name: proxmox_fake_node
proxmox_module_name: default
proxmox_cluster: "1"
proxmox_node: "1"
proxmox_metrics_path: /pve
proxmox_scrape_interval: 15s

proxmox_labels:
  service: proxmox
